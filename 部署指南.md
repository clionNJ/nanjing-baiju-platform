# 南京白局非遗文化数字平台 - 部署指南

## 📋 部署前准备

### 1. 检查文件完整性
确保以下文件都在项目目录中：
```
xiaotiao/
├── index.html          ✅ 主文件
├── images/            ✅ 图片文件夹
│   ├── 4ED9557E923B0B49B6F4A467C16AB514.jpg
│   └── 3DB4A5D6F35C44B34ECBC88BD7D20D37.jpg
├── 白局小明视频.mp4    ✅ 视频文件
└── 使用指南.md
```

### 2. 测试本地运行
在部署前，确保项目在本地可以正常运行：
- 双击 `index.html` 或在本地服务器测试
- 检查所有功能是否正常
- 确保图片和视频能正常加载

---

## 🚀 部署方式一：GitHub Pages（免费，推荐新手）

### 优点
- ✅ 完全免费
- ✅ 操作简单
- ✅ 自动HTTPS
- ✅ 全球CDN加速
- ✅ 支持自定义域名

### 步骤

#### 1. 创建GitHub账号
如果没有，访问 https://github.com 注册

#### 2. 创建新仓库
1. 登录GitHub
2. 点击右上角 "+" → "New repository"
3. 仓库名称：`nanjing-baiju-platform`（或任意名称）
4. 选择 Public（公开）
5. 不要勾选 "Initialize this repository with a README"
6. 点击 "Create repository"

#### 3. 上传项目文件
**方法A：使用Git命令行**
```bash
# 在项目文件夹中打开命令行
cd C:\Users\clion\Desktop\xiaotiao

# 初始化Git仓库
git init

# 添加所有文件
git add .

# 提交文件
git commit -m "Initial commit: 南京白局非遗文化数字平台"

# 添加远程仓库（替换YOUR_USERNAME为你的GitHub用户名）
git remote add origin https://github.com/YOUR_USERNAME/nanjing-baiju-platform.git

# 推送到GitHub
git branch -M main
git push -u origin main
```

**方法B：使用GitHub Desktop（图形界面，更简单）**
1. 下载安装 GitHub Desktop：https://desktop.github.com
2. 打开 GitHub Desktop
3. File → Add Local Repository → 选择项目文件夹
4. 点击 "Publish repository"
5. 输入仓库名称，勾选 "Keep this code private"（可选）
6. 点击 "Publish Repository"

**方法C：直接在GitHub网页上传**
1. 在GitHub仓库页面，点击 "uploading an existing file"
2. 拖拽所有项目文件到页面
3. 点击 "Commit changes"

#### 4. 启用GitHub Pages
1. 进入仓库页面
2. 点击 "Settings"（设置）
3. 左侧菜单找到 "Pages"
4. Source（源）选择 "Deploy from a branch"
5. Branch（分支）选择 "main"，文件夹选择 "/ (root)"
6. 点击 "Save"
7. 等待1-2分钟，页面会显示访问地址：
   ```
   https://YOUR_USERNAME.github.io/nanjing-baiju-platform/
   ```

#### 5. 访问你的网站
部署完成后，访问地址：
```
https://YOUR_USERNAME.github.io/nanjing-baiju-platform/
```

---

## 🌐 部署方式二：Vercel（免费，推荐）

### 优点
- ✅ 完全免费
- ✅ 部署速度快
- ✅ 自动HTTPS
- ✅ 支持自定义域名
- ✅ 自动部署（Git推送后自动更新）

### 步骤

#### 1. 注册Vercel账号
访问 https://vercel.com，使用GitHub账号登录

#### 2. 导入项目
1. 登录后点击 "Add New Project"
2. 选择你的GitHub仓库（需要先上传到GitHub）
3. 点击 "Import"
4. Framework Preset 选择 "Other"
5. 点击 "Deploy"

#### 3. 等待部署
通常30秒-2分钟即可完成

#### 4. 访问网站
部署完成后会显示访问地址：
```
https://nanjing-baiju-platform.vercel.app
```

---

## 🖥️ 部署方式三：Netlify（免费）

### 优点
- ✅ 完全免费
- ✅ 拖拽部署（最简单）
- ✅ 自动HTTPS
- ✅ 支持自定义域名

### 步骤

#### 方法A：拖拽部署（最简单）
1. 访问 https://www.netlify.com
2. 注册/登录账号
3. 在Dashboard页面，找到 "Sites" 区域
4. 直接将项目文件夹拖拽到页面
5. 等待部署完成
6. 获得访问地址：`https://random-name-123456.netlify.app`

#### 方法B：Git连接部署
1. 登录Netlify
2. 点击 "Add new site" → "Import an existing project"
3. 选择 GitHub，授权并选择仓库
4. 点击 "Deploy site"

---

## 💻 部署方式四：自建服务器（需要服务器）

### 适用场景
- 已有服务器（阿里云、腾讯云、AWS等）
- 需要更多控制权
- 需要后端支持

### 步骤

#### 1. 准备服务器
- 购买云服务器（推荐：阿里云、腾讯云、华为云）
- 配置：1核2G内存即可
- 操作系统：Ubuntu 20.04 或 CentOS 7

#### 2. 安装Web服务器

**使用Nginx（推荐）**
```bash
# Ubuntu/Debian
sudo apt update
sudo apt install nginx -y

# CentOS
sudo yum install nginx -y

# 启动Nginx
sudo systemctl start nginx
sudo systemctl enable nginx
```

#### 3. 上传项目文件
**方法A：使用FTP工具（FileZilla）**
1. 下载FileZilla：https://filezilla-project.org
2. 连接服务器（需要IP、用户名、密码）
3. 上传项目文件到 `/var/www/html/` 或 `/usr/share/nginx/html/`

**方法B：使用SCP命令**
```bash
# 在本地项目文件夹执行
scp -r * root@YOUR_SERVER_IP:/var/www/html/
```

**方法C：使用Git**
```bash
# 在服务器上
cd /var/www/html
git clone https://github.com/YOUR_USERNAME/nanjing-baiju-platform.git
```

#### 4. 配置Nginx
```bash
sudo nano /etc/nginx/sites-available/default
```

添加或修改配置：
```nginx
server {
    listen 80;
    server_name YOUR_DOMAIN_OR_IP;
    root /var/www/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

重启Nginx：
```bash
sudo systemctl restart nginx
```

#### 5. 配置防火墙
```bash
# Ubuntu
sudo ufw allow 80
sudo ufw allow 443

# CentOS
sudo firewall-cmd --permanent --add-service=http
sudo firewall-cmd --permanent --add-service=https
sudo firewall-cmd --reload
```

#### 6. 访问网站
通过服务器IP或域名访问：
```
http://YOUR_SERVER_IP
```

---

## 🔒 配置HTTPS（推荐）

### 使用Let's Encrypt免费SSL证书

#### 1. 安装Certbot
```bash
# Ubuntu
sudo apt install certbot python3-certbot-nginx -y

# CentOS
sudo yum install certbot python3-certbot-nginx -y
```

#### 2. 获取证书
```bash
sudo certbot --nginx -d YOUR_DOMAIN
```

#### 3. 自动续期
证书会自动续期，也可以手动测试：
```bash
sudo certbot renew --dry-run
```

---

## 📝 部署方式对比

| 方式 | 难度 | 费用 | 速度 | 推荐度 |
|------|------|------|------|--------|
| GitHub Pages | ⭐⭐ | 免费 | 快 | ⭐⭐⭐⭐⭐ |
| Vercel | ⭐⭐ | 免费 | 很快 | ⭐⭐⭐⭐⭐ |
| Netlify | ⭐ | 免费 | 快 | ⭐⭐⭐⭐⭐ |
| 自建服务器 | ⭐⭐⭐⭐ | 付费 | 中等 | ⭐⭐⭐ |

---

## 🎯 推荐部署流程（新手）

### 最简单的方式（5分钟）
1. **注册GitHub账号**（如果没有）
2. **创建新仓库**并上传项目文件
3. **启用GitHub Pages**
4. **完成！** 获得访问链接

### 详细步骤
```bash
# 1. 在项目文件夹打开命令行
cd C:\Users\clion\Desktop\xiaotiao

# 2. 初始化Git（如果还没初始化）
git init

# 3. 添加所有文件
git add .

# 4. 提交
git commit -m "Deploy: 南京白局非遗文化数字平台"

# 5. 在GitHub创建仓库后，添加远程地址
git remote add origin https://github.com/YOUR_USERNAME/nanjing-baiju-platform.git

# 6. 推送代码
git branch -M main
git push -u origin main

# 7. 在GitHub仓库设置中启用Pages
# Settings → Pages → Source: main branch → Save
```

---

## ⚠️ 部署注意事项

### 1. 文件路径检查
- ✅ 确保所有图片路径使用相对路径（已修复）
- ✅ 视频文件路径正确
- ✅ 不要使用绝对路径（如 `D:/...`）

### 2. 文件大小限制
- **GitHub Pages**: 单个文件建议 < 100MB
- **Vercel/Netlify**: 单个文件建议 < 50MB
- 如果视频文件过大，考虑：
  - 压缩视频
  - 使用视频托管服务（如YouTube、Bilibili）
  - 使用CDN服务

### 3. 浏览器兼容性
- 确保在多种浏览器测试（Chrome、Firefox、Safari、Edge）
- 移动端也要测试

### 4. 性能优化
- 压缩图片（使用 TinyPNG：https://tinypng.com）
- 压缩视频（使用 HandBrake）
- 启用浏览器缓存

---

## 🔧 常见问题解决

### Q1: GitHub Pages显示404错误？
**解决方案：**
- 检查仓库是否为Public
- 确认 `index.html` 在根目录
- 等待几分钟后刷新（部署需要时间）

### Q2: 图片或视频无法加载？
**解决方案：**
- 检查文件路径是否正确（使用相对路径）
- 确认文件已上传到服务器
- 检查文件名大小写（Linux服务器区分大小写）

### Q3: 页面样式错乱？
**解决方案：**
- 检查网络连接（Font Awesome需要网络）
- 清除浏览器缓存
- 检查浏览器控制台错误信息

### Q4: 如何更新网站内容？
**解决方案：**
- 修改本地文件
- 提交并推送到GitHub
- GitHub Pages会自动更新（可能需要几分钟）

---

## 📱 自定义域名（可选）

### GitHub Pages自定义域名
1. 在仓库 Settings → Pages → Custom domain
2. 输入你的域名（如：`baiju.example.com`）
3. 在域名DNS中添加CNAME记录：
   ```
   类型: CNAME
   名称: @ 或 www
   值: YOUR_USERNAME.github.io
   ```

### Vercel/Netlify自定义域名
1. 在项目设置中找到 "Domains"
2. 添加你的域名
3. 按照提示配置DNS记录

---

## 🎉 部署完成检查清单

- [ ] 网站可以正常访问
- [ ] 所有图片正常显示
- [ ] 视频可以正常播放
- [ ] 所有功能按钮正常工作
- [ ] 多语言切换正常
- [ ] 移动端显示正常
- [ ] HTTPS已配置（如果使用自定义域名）

---

## 📞 需要帮助？

如果遇到问题：
1. 查看浏览器控制台（F12）的错误信息
2. 检查服务器日志
3. 参考各平台的官方文档
4. 在GitHub Issues中提问

---

**祝部署顺利！** 🚀

